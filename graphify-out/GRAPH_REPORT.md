# STRATUM — graphify Knowledge Graph Report
> Auto-generated by `graphify .` — do **not** edit by hand.  
> Re-run `graphify .` from the repo root to refresh after structural changes.

---

## 0 · How to read this report

| Symbol | Meaning |
|--------|---------|
| 🔴 God node | Highest import-centrality — changes ripple everywhere |
| 🟠 Hub | High out-degree, frequently imported |
| 🟡 Leaf cluster | Low centrality, safe to edit in isolation |
| → | imports / depends on |
| ←← | imported by (reverse edge) |

---

## 1 · God Nodes  *(touch with extreme care)*

These files have the highest betweenness-centrality in the import graph.  
A change here can break many unrelated surfaces simultaneously.

| Rank | File | Centrality | Why it's a god node |
|------|------|-----------|---------------------|
| 1 | `src/lib/submissionService.ts` | 0.94 | Exports `Attempt`, `startAttempt`, `submitAttempt`, `calculateScores`, `subscribeToAttemptsByAssessment` — imported by ExamShell, ExamBriefingPage, AssessmentRosterCore, ExamResultsPage, and all three roster pages |
| 2 | `src/lib/assessmentService.ts` | 0.91 | Exports `Assessment` type + all CRUD — imported by every builder page, every roster page, ExamBriefingPage, ExamShell, AssessmentRosterCore |
| 3 | `src/lib/firebaseService.ts` | 0.88 | Generic Firestore helpers + all user CRUD (students, faculty, institutes) — imported by all four auth contexts, all roster components, faculty/student management drawers |
| 4 | `src/app/components/assignments/AssessmentRosterCore.tsx` | 0.76 | The **only** shared roster UI — wraps all three role-specific roster pages (Web Owner, Institute, Faculty) |
| 5 | `src/lib/questionBankService.ts` | 0.71 | Exports `Question` type — imported by QuestionPickerModal, QuestionRenderer, ExamShell, calculateScores in submissionService |

---

## 2 · Community Clusters

Graphify detected **7 functional communities** using Louvain modularity.

---

### Community A — Exam Runtime  *(tight coupling, high risk)*
```
ExamShell.tsx
  → submissionService (startAttempt, saveAnswer, submitAttempt, calculateScores, subscribeToAttempt)
  → assessmentService (getAssessment)
  → questionBankService (getQuestions — batch-fetched in chunks of 30)
  → IntegrityEngine.tsx
       → ViolationOverlay.tsx
       → logViolation (submissionService)
  → FaceMonitor.tsx
  → SectionTimer.tsx
       → submitSection (submissionService)
  → QuestionNavigator.tsx
  → QuestionRenderer.tsx
       → questionBankService (Question type)
ExamBriefingPage.tsx
  → assessmentService (getAssessment)
  → submissionService (getAllAttemptsByStudentAndAssessment)
  → StudentAuthContext
ExamResultsPage.tsx
  → submissionService (getAttempt)
  → assessmentService (getAssessment)
  → questionBankService (batch-fetch)
```
**Invariant:** `ExamShell` is the only file that calls both `startAttempt` and `submitAttempt` in the same lifecycle. Never split these calls across components.

---

### Community B — Assessment Builder  *(3 parallel role-mirrors)*
```
pages/AssignmentsPage.tsx          (Web Owner)
pages/institute/InstituteAssignmentsPage.tsx
pages/faculty/FacultyAssignmentsPage.tsx
  └─ all three → assessmentService (CRUD)
  └─ all three → questionBankService
  └─ all three → components/assignments/QuestionPickerModal.tsx
                      → questionBankService
```
**Invariant:** The three builder pages are **structural mirrors** — they share the same UX shape but different `ownerType`/`ownerId`. Any feature added to one must be replicated to the other two.

---

### Community C — Assessment Roster  *(shared core, role wrappers)*
```
components/assignments/AssessmentRosterCore.tsx   ← GOD NODE
  → assessmentService (getAssessment, blockStudent, unblockStudent, setAttemptOverride)
  → firebaseService (getStudentsByInstitute, getAllStudents)
  → submissionService (subscribeToAttemptsByAssessment, getAllAttemptsByStudentAndAssessment, freezeAttempt, unfreezeAttempt)

pages/AssessmentRosterPage.tsx          (Web Owner) → AssessmentRosterCore
pages/institute/InstituteAssessmentRosterPage.tsx → AssessmentRosterCore
pages/faculty/FacultyAssessmentRosterPage.tsx     → AssessmentRosterCore
```
**Next features to add here:**  
- Response viewer (Correct / Wrong / Partial / Unattempted / Text tabs)  
- Soft-delete attempt (`isDeleted` flag, filtered from roster)  
- Override badge on roster rows  

---

### Community D — Auth Contexts  *(fully isolated, never cross-import)*
```
context/AuthContext.tsx           → firebaseService
context/InstituteAuthContext.tsx  → firebaseService
context/FacultyAuthContext.tsx    → firebaseService, emailService
context/StudentAuthContext.tsx    → firebaseService, emailService
```
**Invariant:** These four contexts are hermetically isolated. `AuthContext` must never import `FacultyAuthContext` or `StudentAuthContext`. Role-crossing import = critical bug.

---

### Community E — Question Bank  *(shared across roles via permission gate)*
```
pages/QuestionsPage.tsx
pages/institute/InstituteQuestionsPage.tsx
pages/faculty/FacultyQuestionsPage.tsx
  └─ all → questionBankService
  └─ all → questionShareService
  └─ all → subjectService
  └─ all → components/questions/{BulkUploadModal, ExportModal, QuestionPreview,
                                  QuestionTypeEngine, MathToolbar, RichText,
                                  SubjectCombobox, SubjectManager, ImageUploader}
```

---

### Community F — Institute Hierarchy  *(admin-only, deep sub-tree)*
```
pages/UserManagementPage.tsx
pages/InstituteDetailPage.tsx
  └─ → firebaseService
  └─ → components/schools/{SchoolsTab, HierarchyPanel, HierarchyBreadcrumb,
                             NodeDrawer, CourseDrawer, SemesterDrawer,
                             NodeStudentRoster, StudentMappingDrawer}
  └─ → components/faculty/{FacultyTab, AddFacultyDrawer, BulkFacultyModal}
  └─ → components/student/{StudentTab, AddStudentDrawer, BulkStudentModal}
```

---

### Community G — Route Shell + Layouts  *(entry points, rarely edited)*
```
App.tsx → RouterProvider → routes.tsx
routes.tsx → Root, InstituteRoot, FacultyRoot, StudentRoot
Root.tsx (web-owner shell)
FacultyRoot.tsx  → FacultyAuthContext
InstituteRoot.tsx → InstituteAuthContext
StudentRoot.tsx → StudentAuthContext

Layouts (each wraps Outlet with auth guard + nav):
  DashboardLayout.tsx          → AuthContext
  InstituteDashboardLayout.tsx → InstituteAuthContext
  FacultyDashboardLayout.tsx   → FacultyAuthContext
  StudentDashboardLayout.tsx   → StudentAuthContext
```

---

## 3 · Critical Paths

### Path: "student starts an exam"
```
StudentAssessmentsPage
  → ExamBriefingPage
       reads: getAssessment, getAllAttemptsByStudentAndAssessment
       checks: maxAttempts, attemptOverrides, blockedStudents, status
  → ExamShell
       calls: startAttempt (throws ATTEMPT_LIMIT_EXCEEDED if over limit)
       calls: registerSession (dual-device guard)
       streams: subscribeToAttempt (freeze/unfreeze watch)
       saves: saveAnswer (debounced ~1.5 s)
       submits: submitAttempt → calculateScores
  → ExamResultsPage
       reads: getAttempt, getAssessment, batch-fetch questions
```

### Path: "faculty checks a live attempt"
```
FacultyAssessmentRosterPage
  → AssessmentRosterCore
       streams: subscribeToAttemptsByAssessment (onSnapshot)
       reads: getAssessment (once on mount)
       reads: getStudentsByInstitute (once on mount)
       actions: freezeAttempt / unfreezeAttempt / blockStudent / unblockStudent / setAttemptOverride
```

### Path: "response viewer" *(next to build)*
```
AssessmentRosterCore (student drawer)
  → batch-fetch Question docs in chunks of 30
       questionBankService.getQuestionsByIds(ids: string[])   ← add if missing
  → cross-reference attempt.answers[questionId] with Question.correctIds / correctPairs
  → classify each answer: Correct | Wrong | Partial | Unattempted | Text
  → render tab UI inside the student detail drawer
```

### Path: "soft-delete attempt" *(next to build)*
```
submissionService.softDeleteAttempt(attemptId)
  → updateDoc attempts/{id} { isDeleted: true }

AssessmentRosterCore
  → filter subscribeToAttemptsByAssessment results: exclude isDeleted === true from active roster
  → show deleted attempts in drawer "History" section with struck-through styling
```

---

## 4 · Firestore Access Patterns

| Operation | Collection | Where clauses | Chunk size |
|-----------|-----------|---------------|-----------|
| Batch-fetch questions by ID | `questions` | `id in [...]` | **30** (Firestore `in` cap) |
| Attempts for a roster | `attempts` | `assessmentId == x` | — (onSnapshot) |
| All attempts by student+assessment | `attempts` | `studentId == x`, `assessmentId == y` | — |
| Assessments by owner | `assessments` | `ownerType == x`, `ownerId == y`, `isDeleted == false` | — |

**Rule:** Never call Firestore directly from a page or component. Always route through `/src/lib/*.ts` service files.

---

## 5 · Pending Feature Hooks

### (1) Response Viewer ✅ DONE
- `getQuestionsByIds` added to `questionBankService.ts` — chunks of 30
- `ResponseViewer` component added to `AssessmentRosterCore.tsx`
- Tabs: `Correct` | `Wrong` | `Partial` | `Unattempted` | `Text`
- Expandable per-question rows with student answer, correct answer, and explanation
- Classification mirrors `scoreMCQ` / `scoreMatch` logic from `submissionService.ts`

### (2) Soft-Delete Attempt ✅ DONE
- `softDeleteAttempt(attemptId)` added to `submissionService.ts`
- `isDeleted?: boolean` field added to `Attempt` type
- Roster subscription filters `isDeleted === true` from active view
- History section shows deleted attempts with red "Deleted" badge + strikethrough + 65% opacity
- `SoftDeleteConfirmModal` added; delete button appears on each completed, non-deleted attempt row
- Delete is surfaced via `Trash2` icon button in `AttemptsPanel` history rows

### (3) Override Badge on Roster Rows ✅ DONE
- `attemptOverrides?: Record<string, number>` prop added to `RosterTableRow`
- Blue `#N` pill (Hash icon + value) shown in badges column when `attemptOverrides[student.id]` is defined
- Passed down from main component via `assessment.attemptOverrides`

---

## 6 · Files Excluded (see `/.graphifyignore`)

- `node_modules/`
- `dist/`
- `src/app/components/ui/` (shadcn/ui primitives — low signal)
- `*.css`, `*.md`, `*.json`, `*.rules`
- `supabase/`
- `utils/`

---

*Generated: 2026-04-14 · STRATUM v0.9 · graphify 0.4.x*
